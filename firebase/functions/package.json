{
  "name": "tracenet-functions",
  "private": true,
  "type": "commonjs",
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "test": "vitest run --config vitest.config.ts",
    "test:integration": "npm run build && firebase emulators:exec --project tracenet-emulator-test --only auth,firestore,functions \"vitest run --config test-emulator/vitest.config.ts\""
  },
  "dependencies": {
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^7.3.2"
  },
  "devDependencies": {
    "firebase": "^11.10.0",
    "firebase-functions-test": "^3.4.1",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
